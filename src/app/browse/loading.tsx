import { BrowseSkeleton } from '@/features/catalog/components/BrowseSkeleton';

/** Route-level loading state for /browse (Section 3). */
export default function BrowseLoading() {
  return <BrowseSkeleton />;
}
